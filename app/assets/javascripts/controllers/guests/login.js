(function(_) {
  function LoginController() {
  }

  var fn = LoginController.prototype;
      app = angular.module('guests/login', []);

  app.controller('LoginController', [LoginController]);
})(window._);
