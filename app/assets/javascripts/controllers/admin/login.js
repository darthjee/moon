(function(_, angular) {
  function AdminLoginEditController() {
  }

  var fn = AdminLoginEditController.prototype,
      app = angular.module('admin/login/edit', []);

  app.controller('AdminLoginEditController', [
    AdminLoginEditController
  ]);
})(window._, window.angular);
