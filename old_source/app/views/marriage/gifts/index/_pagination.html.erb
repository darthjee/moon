<%= render partial: 'marriage/utils/pagination', locals: {
  controller: :glc,
  path_method: :paginated_marriage_gifts,
  options: {
    sort_by: '{{ glc.params.sort_by }}',
    sort_direction: '{{ glc.params.sort_direction }}'
  }
} %>

